{
  "name": "blok-teck-package",
  "version": "1.0.0",
  "description": "Information about my kdrama matching feature",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "Kdrama",
    "precompress": "{{ executes BEFORE the `compress` script }}",
    "compress": "{{ run command to compress files }}",
    "postcompress": "{{ executes AFTER `compress` script }}",
    "lint": "./node_modules/.bin/eslint *.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xiaonanpols21/block-tech-v2.git"
  },
  "keywords": [
    "kdrama"
  ],
  "author": "Xiao Nan Pols",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.1",
    "ejs": "^3.1.7",
    "eslint-config-p5js": "^1.1.0",
    "express": "^4.17.3",
    "mongodb": "^4.6.0"
  },
  "devDependencies": {
    "eslint": "^8.15.0",
    "nodemon": "^2.0.16"
  }
}
